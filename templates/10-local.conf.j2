# provides UDP syslog reception
$ModLoad imudp
$UDPServerRun 514

# provides TCP syslog reception
$ModLoad imtcp
$InputTCPServerRun 514

$template RemoteLog,"{{ remotelog_directory }}/{{ remotelog_basename }}"
:fromhost-ip,!isequal,"127.0.0.1" ?RemoteLog
& ~
